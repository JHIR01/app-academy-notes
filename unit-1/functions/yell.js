/**
 * function that takes in a string and returns a "yelled" 
 * version of that string.
 */
function yell(sentence){
    return sentence.toUpperCase() + "!!!";
}

console.log(yell("I want to go to the store"));
console.log(yell("Time to program"));