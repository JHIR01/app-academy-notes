/**
 * write a function 'catBuilder(name, color, type)' that returns a cat object with 
 * the corresponding properties.
 */
function catBuilder(cat){
    var tempCat = {
        name: name,
        color: color,
        toys: toys
    };
}

var tempName = prompt("Please enter in your cat's name: ");
console.log(tempName);